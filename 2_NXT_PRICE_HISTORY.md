# [2단계] 넥스트레이드 30분봉 OHLC 수집 & 세션 취합

> 진행 순서: [1] Universe([1_NXT_UNIVERSE.md](1_NXT_UNIVERSE.md)) → **[2] 30분봉 수집(이 문서)** → [3] 분석([3_NXT_ANALYSIS.md](3_NXT_ANALYSIS.md)).
>
> **상태: 라이브 검증 완료** (2026-05-31, `lseg.data 2.1.1`, 005930.KNT 실데이터로 확인)

## 1. 목표

넥스트레이드에서 거래되는 종목의 **프리장 / 정규장 / 애프터장별 시가·고가(및 저가)** 를 구한다.
LSEG API에는 "프리장/애프터장"에 해당하는 직접 필터가 없으므로,
**30분봉(`interval="30min"`)을 받아 → KST로 변환 → 세션 시간대로 버킷팅 → 취합**한다.

## 2. 호출 방법

```python
import lseg.data as ld
ld.open_session()                       # Workspace 데스크톱 앱 실행 중이어야 함

df = ld.get_history(
    universe="005930.KNT",              # ★ .KNT = 넥스트레이드(NXT) venue
    fields=["OPEN_PRC", "HIGH_1", "LOW_1", "TRDPRC_1", "ACVOL_UNS"],
    interval="30min",
    start="2026-05-29",                 # 'YYYY-MM-DD' 또는 ISO ('...T22:00:00')
    end="2026-05-30",
)
```

### 2.1 venue 비교 (실측 — 2026-05-29)

같은 날 같은 종목을 두 RIC으로 받았을 때:

| RIC | 반환 봉 수 | 시간 범위(KST) | 비고 |
|---|---|---|---|
| **`005930.KNT`** (넥스트레이드) | **24** | **08:00 ~ 19:30** | 프리 + 정규 + 애프터 전부 포함 ✅ |
| `005930.KS` (KRX/KOSPI) | 15 | ~15:30까지 | 이후 `<NA>`. KRX는 연장세션 없음 |

→ **프리/애프터 데이터는 `.KNT`에만 존재.**

### 2.2 인트라데이 한도 (실측 — 2026-05-31, 005930.KNT)

30분봉은 **두 개의 독립적인 한계**가 있다 (점점 긴 윈도우를 요청해 반환 봉 수가 멈추는 지점·실제 시작일로 실측):

| 한계 | 값 | 동작 |
|---|---|---|
| **① 가용 시작점 (롤링 리텐션)** | **KST 2025-06-02 ~** | 그 이전은 `end`를 당겨도 못 받음(2025-04-15 종료 요청 → EMPTY). 약 12개월만 보존 |
| **② 1회 반환 봉 한도** | **≈ 5,778봉** (240거래일 ≈ 11개월) | 초과 요청 시 **에러 없이 최신쪽만 주고 과거를 조용히 절단** |

- **NXT 출범(2025-03)~5월 30분봉은 이미 소실**(롤링). 인트라데이 분석 가능 구간 = **2025-06 이후**.
  리텐션 floor는 매일 전진하므로 **빨리 수집할수록 더 깊은 과거 확보**. (출범 첫 구간은 일봉으로만 접근 가능할 수 있음 — 미확인.)
- 005930(최유동)은 봉이 가장 빽빽해 위가 보수적 상한. 유동성 낮은 종목은 같은 5,778봉 한도가 더 먼 과거까지 닿는다.

#### 절단 방지 패턴 (필수)

초과 시 예외가 아니라 **조용히 잘리므로**, 호출 후 반드시 실제 시작을 검증한다:

```python
df = ld.get_history(universe=ric, interval="30min", start=start, end=end, fields=...)
actual = pd.to_datetime(df.index).min()
if actual.date() > pd.Timestamp(start).date():   # 요청보다 늦게 시작 → 절단
    ...  # 윈도우를 더 짧게 쪼개 재호출
```

→ 권장: 가용 전체(~12개월·~5,778봉)는 한도에 거의 꽉 차므로 **6개월/회 × 2청크**로 받아 절단을 피한다.

### 2.3 수집 윈도우 & 컷 기준 (실제 적용 — [code/collect_all.py](code/collect_all.py))

| 항목 | 값 |
|---|---|
| **기간** | `2025-06-01` ~ `2026-05-30` (가용 전체 ≈ 12개월) |
| **청크** | 6개월 2분할: `2025-06-01~2025-12-02`, `2025-12-01~2026-05-30` (경계 1일 겹침 → 인덱스로 dedup) |
| **컷(절단) 기준** | 반환 봉이 `5,700`(`BAR_CAP`) 이상이면 윈도우를 반으로 쪼개 재귀 호출(최대 depth 4) |
| **스로틀** | 요청 간 `0.4s` (5 req/s + 분당 데이터 한도 여유) |
| **재개** | 종목당 `data/raw/{code}.parquet` 저장 → 이미 있으면 skip. `429`(일일 한도) 시 안전 중단 후 재실행으로 재개 |

> 005930(최유동)은 12개월이 ~5,800봉이라 단일 호출이면 잘리지만, 위 분할로 온전히 확보됨(실측).

## 3. 반환 데이터

### 3.1 필드 (실측 확인)

| 필드 | 의미 |
|---|---|
| `OPEN_PRC` | 시가 |
| `HIGH_1` | 고가 |
| `LOW_1` | 저가 |
| `TRDPRC_1` | 종가(마지막 체결가) |
| `ACVOL_UNS` | **해당 봉 구간 거래량** (누적 아님 — §4.1 값이 봉마다 증감하는 것으로 확인). 거래대금 근사에 사용 |

> `fields`를 지정하지 않으면 기본 17개 컬럼이 따라온다:
> `HIGH_1, LOW_1, OPEN_PRC, TRDPRC_1, NUM_MOVES, ACVOL_UNS, VWAP,
> BID_HIGH_1, BID_LOW_1, OPEN_BID, BID, BID_NUMMOV, ASK_HIGH_1, ASK_LOW_1, OPEN_ASK, ASK, ASK_NUMMOV`.
> 필요한 5개만 명시하는 것을 권장.

### 3.2 타임스탬프 = naive UTC

- 인덱스 이름은 `Timestamp`, **타임존 정보 없는 UTC**로 반환된다(`tz=None`).
- KST 변환: `df.index.tz_localize("UTC").tz_convert("Asia/Seoul")`
- 예: UTC `2026-05-29 00:00:00` → KST `2026-05-29 09:00:00` (정규장 시작).

## 4. 세션 취합 결과 (실측 — 005930.KNT, 2026-05-29)

세션 경계(KST): 프리 `~09:00`, 정규 `09:00~15:30`, 애프터 `15:30~`.
(세션 open = 해당 세션 **첫 봉의 `OPEN_PRC`**, high = 세션 내 `HIGH_1`의 **max**, low = `LOW_1`의 **min**)

| 세션 | 시가(open) | 고가(high) | 저가(low) | 봉 수 | 시간(KST) |
|---|---|---|---|---|---|
| 프리(pre) | 303,000 | 311,000 | 303,000 | 2 | 08:00–08:30 |
| 정규(regular) | 310,000 | **319,000** | 305,500 | 13 | 09:00–15:00 |
| 애프터(after) | 317,000 | 318,500 | 316,000 | 9 | 15:30–19:30 |

### 4.1 원자료 (30분봉 24개, UTC 기준)

```
Timestamp(UTC),OPEN_PRC,HIGH_1,LOW_1,TRDPRC_1,ACVOL_UNS
2026-05-28 23:00:00,303000,309500,303000,307500,4000031   # KST 08:00 (프리)
2026-05-28 23:30:00,308000,311000,307500,310000,1744412   # KST 08:30 (프리)
2026-05-29 00:00:00,310000,319000,308500,310500,4616071   # KST 09:00 (정규 시작)
2026-05-29 00:30:00,310500,313000,308000,308500,1279196
2026-05-29 01:00:00,308000,312500,305500,312500,1208759
2026-05-29 01:30:00,312500,314000,311000,313500,1164379
2026-05-29 02:00:00,313500,314500,310000,310500,783823
2026-05-29 02:30:00,310500,312000,309000,310500,576128
2026-05-29 03:00:00,310500,313500,310000,311500,522045
2026-05-29 03:30:00,311500,313000,311000,312500,388821
2026-05-29 04:00:00,312500,313500,312000,312500,544211
2026-05-29 04:30:00,313000,314500,312500,314000,819056
2026-05-29 05:00:00,314000,317000,313500,317000,1216320
2026-05-29 05:30:00,316500,318500,315250,317500,1511695
2026-05-29 06:00:00,317500,318000,316000,317000,660141    # KST 15:00 (정규 마지막 봉)
2026-05-29 06:30:00,317000,317500,316000,316500,463553    # KST 15:30 (애프터 시작)
2026-05-29 07:00:00,316500,317000,316000,316000,306872
2026-05-29 07:30:00,316000,316500,315500,316000,283522
2026-05-29 08:00:00,316500,317000,316000,317000,245706
2026-05-29 08:30:00,317000,318000,316500,318000,502421
2026-05-29 09:00:00,317500,318500,317500,318000,640332
2026-05-29 09:30:00,318500,318500,318000,318500,228614
2026-05-29 10:00:00,318000,318500,318000,318500,469044
2026-05-29 10:30:00,318000,318500,317500,318500,942419    # KST 19:30 (애프터)
```

## 5. 구현

- **[code/collect_all.py](code/collect_all.py)** — 전체 universe 일괄 수집(§2.3: 청크·절단가드·스로틀·재개). 실제 수집은 이걸로.
- [code/nxt_price_history.py](code/nxt_price_history.py) — 단일 종목 참조 구현(`get_nxt_30min()` + `aggregate_sessions()`)
  및 **세션 경계 정의(`_session()`, `OHLC_FIELDS`)** — collect_all/metrics가 import.

실행:
```powershell
python code/collect_all.py            # 전체
python code/collect_all.py --limit 3  # 앞 3종목 스모크 테스트
python code/collect_all.py --codes 005930 000660
```

## 6. 주의사항

1. **세션 경계는 넥스트레이드 공식 운영시간으로 확정 필요.**
   데이터상 거래는 08:00 KST(프리마켓)부터 잡힌다. NXT 공식 운영시간
   (프리 08:00~08:50 / 메인 09:00~15:30 / 애프터 15:30~20:00) 기준으로
   [code/nxt_price_history.py](code/nxt_price_history.py)의 `_session()` 경계만 조정하면 된다.
   단일가(개시/종가) 구간을 어느 세션에 넣을지는 분석 목적에 맞게 결정.
   - ⚠️ **특수 운영시간일 보정**: 고정 시계로만 끊으면 **+1시간 순연일**이 오분류된다.
     실측: **2025-11-13(수능)** 개장·폐장 +1h(첫 봉 10:00, 본장 ~16:30) → 15:30~16:30이 실은
     본장인데 '애프터'로 잘못 잡힘. **2026-01-02(신년 개장)** 개장만 10:00(폐장 정상).
     `_session()`의 `SPECIAL_BOUNDS`에 날짜별 경계를 넣어 처리(수능일 등록 완료).
     수집된 parquet의 `session` 컬럼은 인덱스에서 재라벨하면 재다운로드 없이 보정된다.
2. **장기 구간은 분할 호출 + 절단 검증.** 1회 ≈5,778봉 한도(§2.2)가 있고
   초과분은 조용히 잘리므로, 6개월/회로 쪼개고 호출마다 실제 시작일을 검증한다.
   가용 시작점은 KST 2025-06-02(그 이전 30분봉 없음).
3. **API 한도** (기존 프로젝트 문서 기준, 전 API 공유): 5 req/sec, 일 10,000 calls,
   일 5GB, 분당 50MB. → [source_docs/TECHNICAL_REFERENCE.md](source_docs/TECHNICAL_REFERENCE.md) §1.1.
4. **다종목**은 종목별 순회 + 재시도 패턴 권장(인트라데이는 데이터량이 커서 한 번에 많은
   universe를 넣으면 분당 데이터 한도에 걸리기 쉬움).
