# [3단계] 프리/정규/애프터 '튐' 분석

> 진행 순서: [1] Universe([1_NXT_UNIVERSE.md](1_NXT_UNIVERSE.md)) → [2] 30분봉 수집([2_NXT_PRICE_HISTORY.md](2_NXT_PRICE_HISTORY.md)) → **[3] 분석(이 문서)**.

## 1. 목표

**넥스트레이드의 프리장/애프터장에서 가격이 (정규장 대비) 튀는가?** 를 검증한다.
KRX엔 프리/애프터가 없으므로 이 질문은 NXT(`.KNT`) 데이터로만 가능하다.

[code/metrics.py](code/metrics.py)가 `data/raw/*.parquet`(2단계 산출)를 읽어,
**(종목 × 날짜)** 한 행에 세션별 지표 + 세션 간 지표를 붙인 wide 패널
`data/panel_sessions.{parquet,csv}`을 만든다.

## 2. '튐'의 정의 — 4개 기준 (A/B/C/D)

세션 경계(KST): 프리 `~09:00`, 정규 `09:00~15:30`, 애프터 `15:30~` (`_session()`).
세션 수익률 = `close/open − 1`. 봉별 typical = `(고+저+종)/3`, 거래대금 ≈ `typical × 거래량(ACVOL_UNS)`.

### A. 세션 간 갭 (gap) — 세션이 바뀔 때의 가격 점프(수익률)

| 컬럼 | 정의 |
|---|---|
| `gap_overnight_pre` | 프리 시가 / **전일** 정규 종가 − 1 |
| `gap_pre_reg` | 정규 시가 / 프리 종가 − 1 |
| `gap_reg_aft` | 애프터 시가 / 정규 종가 − 1 |
| `gap_aft_nextreg` | **익일** 정규 시가 / 애프터 종가 − 1 |

### B. 세션 내 변동성 — 한 세션 안에서 얼마나 출렁였나

| 컬럼 | 정의 |
|---|---|
| `{pre,reg,aft}_range_pct` | (세션 고가 − 저가) / 세션 시가 |
| `{pre,reg,aft}_ret_std` | 세션 내 봉별 수익률(`TRDPRC_1.pct_change`)의 표준편차 |

### C. 튀고 되돌림 (revert) — 연장세션에서 튄 가격이 정규장에서 제자리로?

| 컬럼 | 정의 |
|---|---|
| `aft_drift` | 애프터 종가 / 정규 종가 − 1 (애프터가 정규종가에서 벗어난 정도) |
| `aft_revert_frac` | 그 드리프트가 **익일 정규 시가**까지 되돌린 비율 (`1`=완전 되돌림, `0`=지속, 음수=오히려 확대) |
| `pre_drift` | 프리 종가 / 전일 정규 종가 − 1 |
| `pre_revert_frac` | 그 드리프트가 **당일 정규 종가**까지 되돌린 비율 |

### D. 거래량 대비 과민 (Amihud 비유동성)

| 컬럼 | 정의 |
|---|---|
| `{pre,reg,aft}_amihud` | \|세션수익률\| / 세션 거래대금 — 클수록 적은 거래로 가격이 크게 움직임(잘 튐) |

그 외 세션별 원지표: `{pre,reg,aft}_{open,high,low,close,vwap,volume,turnover,bars}`.

## 3. 실행

```powershell
python code/metrics.py                 # data/raw 전체 → 패널 + 세션별 요약 출력
python code/metrics.py --codes 005930  # 특정 종목만
```

산출: `data/panel_sessions.parquet`, `data/panel_sessions.csv` (행 = 종목×날짜).

## 4. 해석 시 반드시 보정할 함정 (3종목 테스트에서 확인)

1. **세션 길이 미보정 → `range_pct` 직접 비교 금지.**
   정규(6.5h) > 애프터(4.5h) > 프리(1h)로 구간 길이가 달라, 길이가 긴 정규의 range가
   당연히 커 보인다(테스트: 정규 3.2% > 프리 1.8% > 애프터 0.8% — 착시). 세션 간 비교는
   **봉당(30분당) 변동성**(예: `range_pct / sqrt(bars)`, 또는 봉별 \|수익률\| 분포)으로 정규화해야 공정.
2. **프리 `ret_std`는 구조적으로 빈약.** 프리는 하루 ~2봉뿐 → 봉별 수익률이 1개라 일중 표준편차가 `NA`.
   프리 변동성은 `range_pct`나 **여러 날 풀링**으로 봐야 한다.
3. **D는 전체 universe에서야 드러난다.** 대형 유동주는 Amihud가 다 낮아 차이가 안 보임.
   유동성 **상/중/하 층화** 후 비교해야 "얇을수록 튄다" 가설을 검증할 수 있다.

## 5. 본 분석 — [code/analyze.py](code/analyze.py)

§4 함정을 보정한 공정 분석. 입력: `data/raw/*.parquet` + `data/panel_sessions.parquet`.
출력: **`data/analysis_summary.md`**(결과 표) + `data/analysis_*.csv`.

```powershell
python code/analyze.py
```

수행: (B) 봉당 |30분 수익률|로 길이 중립 변동성, (A) 갭 분포, (C) 큰 드리프트의 되돌림,
(D) 유동성 3분위 × 세션 Amihud.

### 핵심 결과 (993종목, 2025-06~2026-05)

- **B**: 봉당 변동성은 정규 ≥ 프리 > 애프터(애프터 0.52×). 시간당으론 연장세션이 더 잠잠 — range% 착시 정정.
- **A**: 프리↔정규(0.10%)·정규↔애프터(0.14%) 핸드오프는 거의 연속. 큰 점프는 오버나잇 갭(p99 7~8%)에 집중.
- **C**: 애프터 드리프트는 대체로 지속(되돌림 중앙 0.00), 프리는 일부 되돌림(0.27~0.35).
- **D ⭐**: 유동성 낮을수록 프리장 Amihud가 정규의 **최대 5.5배** → '얇을수록 튄다'는 프리장에서 확인.

→ **결론**: 프리/애프터는 평균적으로 정규보다 차분·연속적. '튐'은 (1) 오버나잇 갭 꼬리, (2) 저유동성 종목의 프리장에 집중.

## 6. 케이스 발굴 (기사용) — [code/case_finder.py](code/case_finder.py)

"○○가 프리장에서 N% 급락했다 즉시 복원" 류 **개별 사건**을 찾는다.
정규 VWAP 기준선 방식은 **방향성 급락/급등을 '복원'으로 오분류**하는 결함이 있어 폐기하고,
**봉 자체 모양**으로 '튀었다 제자리'를 잡는다(외부 기준선·세션경계 불요 → 기업행위·수능순연에 견고):

- **intra-bar 꼬리**: `up_wick=(고−max(시,종))/시`, `down_wick=(min(시,종)−저)/시` → 한 30분봉 안 왕복.
- **인접봉 V자**: 봉 N 종가가 양옆 봉에서 벗어났고 양옆은 서로 비슷 → 30분 경계 걸친 플래시.
- 점수 = 두 값의 max. 출력: `data/jump_cases.csv` + `data/figs/case_*.png`.

```powershell
python code/case_finder.py [--min-exc 0.05] [--top 40] [--plot 12] [--sessions pre after]
```

### 검증된 대표 사례 (프리장, 라이브 확인)

| 종목 | 날짜 | 봉 | 움직임 | 비고 |
|---|---|---|---|---|
| **한미반도체**(042700) | 2025-07-14 | 08:00 | 94,100 → **66,800(−29%)** → 94,500 | 유동성 최상위(0.99)도 발생 |
| **엘앤에프**(066970) | 2026-02-03 | 08:00 | 135,000 → **86,400(−31%)** → 128,300 | |
| **하나마이크론**(067310) | 2025-10-13 | 08:00 | 23,700 → **31,650(+33%)** → 23,300 | 급등 후 복원 |

### 케이스 규모·방향 분포 (전 종목 1년, 종목×일×세션 최대 spike 기준)

| 임계 | 총 | 상승 | 하락 | 프리 | 애프터 |
|---|---|---|---|---|---|
| ≥5% | 2,595 | 1,864 | 731 | 2,001 | 594 |
| **≥10% (분석 기준)** | **401** | 260 | 141 | 332 | 69 |
| ≥20% | 95 | 34 | 61 | 92 | 3 |

→ **압도적 프리장**(≥10%의 83%). 중간 강도는 **상승**이 많지만 **극단(≥20%)은 하락(크래시) 우세**.
   본 분석/차트는 **≥10% 전수 401건**을 표준 케이스셋으로 사용(`jump_cases.csv`).

## 7. 빈도 랭킹 + KRX 대조 + 기사용 차트

### 7.1 종목별 '튐' 빈도 — [code/jumpiness.py](code/jumpiness.py)
`data/jumpiness_ranking.csv`. 종목별 "지난 1년 프리장 N% 플래시 며칠".
→ **993종목 중 264종목이 프리장 10%+ 플래시를 1회 이상**, 대상홀딩스·ITM반도체 등은 상습(3일+).

```powershell
python code/jumpiness.py [--session pre|after] [--rank-thr 0.10]
```

### 7.2 KRX 대조군 — [code/krx_control.py](code/krx_control.py)
케이스만 KRX(`.KS`/`.KQ`) 정규장 타깃 수집 → `data/krx_cases.parquet` + `data/krx_corroboration.csv`.
→ **≥10% 전수 401건 중 386/399(97%)**, NXT 프리/애프터 극단가가 KRX 당일 정규장 범위 '밖'
(spike 클수록 100%에 수렴). 즉 그 가격은 KRX엔 없던 **NXT 연장세션 전용 현상**.
(현대차 NXT 프리 139,300 vs KRX저가 196,200 / 한미반도체 66,800 vs 89,200 등. 2건은 KRX 미상장.)

**재개형**: 기존 캐시의 `(code,date)`는 건너뛰고 신규만 수집 → `--top`을 늘려도 추가분만 호출.

```powershell
python code/krx_control.py [--top 401] [--refetch]   # LSEG Workspace 필요
```

### 7.3 기사용 차트 — [code/plot_cases.py](code/plot_cases.py)
`data/figs/news_*.png` (401장): NXT 일중봉 + 플래시 강조 + **KRX 정규장 종가/범위 오버레이**.
프리 급변선이 KRX 범위 밖에 동떨어져 보임 → "연장세션 유동성 부족"이 한눈에.

```powershell
python code/plot_cases.py [--top 401]
```

## 8. 빈도 × 시총/섹터 교차 — [code/cross_analysis.py](code/cross_analysis.py)

`data/cross_sector.csv`, `data/cross_mktcap.csv` (종목 메타는 `.KS/.KQ` 시총·섹터를 받아 `data/stock_meta.csv` 캐싱).

### 시총 5분위 × 프리장 10%+ 플래시 종목 비율
| 분위 | 종목 | 플래시종목 | 비율 |
|---|---|---|---|
| Q1(소형) | 199 | 32 | **16%** |
| Q2 | 198 | 53 | 27% |
| Q3 | 199 | 54 | 27% |
| Q4 | 198 | 65 | **33%** |
| Q5(대형) | 199 | 60 | 30% |

→ ⚠️ **반직관**: 소형주가 가장 낮고 중·대형(Q4/Q5)이 더 높다. **플래시는 소형주만의 문제가 아니다.**
   (단, 이 '빈도'는 **프리장에서 실제 체결이 있어야 기록**되므로, 프리 거래가 적은 초소형주는 적게 잡힌다.
   §5의 Amihud(거래량당 민감도)와는 다른 렌즈 — "거래당 취약성"은 소형주가 높고, "관측 빈도"는 중·대형이 높음.)

### 섹터별 (취약 순, 프리 10%+ 플래시 종목 비율)
유틸리티 55% · 에너지 47% · 필수소비재 33% · 금융 30% · 기술 29% … 소재 20%.
절대 건수는 **기술(96건, 251종목)**이 최다.

→ **기사 함의**: "넥스트레이드 프리장 플래시는 소형주 한정이 아니라 현대차·한미반도체 등 **중·대형주에서도 빈번**"이 더 강한 메시지.

## 9. 더 볼 것
- 정규장 대비 유의성 검정(부트스트랩/Mann-Whitney).
- 프리장 체결 빈도로 정규화한 '플래시 발생률'(거래 기회 대비) → 빈도 교란 보정.
